import DefaultTheme from 'vitepress/theme'
import '../../../_shared/docs-theme.css'

// User guide uses the default VitePress theme + the shared warm-clay palette and
// responsive tweaks (docs/_shared/docs-theme.css).
export default DefaultTheme

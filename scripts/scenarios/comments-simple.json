{
  "name": "comments-simple",
  "description": "IXT-SUITES: pinned design comments (pro). Hidden in Simple, present in Pro; the panel (#commentsPanel) mounts, addComment pins a note (rendered in-scene + listed), it can be resolved. Mobile leg; back-to-Simple hides it.",
  "url": "http://localhost:5208/",
  "steps": [
    { "name": "store-ready", "waitFor": { "storeExists": true }, "timeout": 60000 },
    { "name": "boot-splash-gone", "waitFor": { "css": "#boot-loader", "visible": false }, "timeout": 60000, "failMessage": "#boot-loader still covering the screen. It is removed from the DOM only once `booting` clears, which depends on sceneReady \u2014 measured 751ms and 36604ms across two runs of the SAME scenario, so no fixed wait covers it. Every screenshot before this point is the loader, and nothing fails (v0.31.9.6)." },
    { "name": "dismiss-overlays", "eval": "try { localStorage.setItem('hdb_onboarded','1') } catch {}; const s = window.__store.getState(); s.endTour?.(); s.setOnboardingOpen?.(false); s.dismissLocationPrompt?.()" },
    { "name": "set-daytime", "eval": "window.__store.getState().setManualHour?.(13)" },
    { "name": "assert-simple-hidden", "waitFor": { "store": "state.uiMode === 'simple' && state.featureFlags.comments === false" }, "timeout": 5000, "failMessage": "comments should be OFF in Simple mode" },
    { "name": "to-pro", "eval": "(()=>{const s=window.__store.getState(); s.setUiMode('pro'); s.reresolveFeatureFlags();})()" },
    { "name": "assert-pro-on", "waitFor": { "store": "state.featureFlags.comments === true" }, "timeout": 5000, "failMessage": "comments should be ON in Pro mode" },
    { "name": "open-panel", "store": { "action": "setCommentsOpen", "args": [true] } },
    { "name": "panel-visible", "waitFor": { "css": "#commentsPanel" }, "timeout": 8000, "failMessage": "Comments panel did not mount" },
    { "name": "assert-title", "waitFor": { "text": "Comments" }, "timeout": 5000 },
    { "name": "add-comment", "eval": "(()=>{window.__store.getState().addComment({position:[8,2.5], text:'Move the sofa left'});})()" },
    { "name": "assert-added", "waitFor": { "store": "state.comments.length === 1 && state.comments[0].text === 'Move the sofa left'" }, "timeout": 5000, "failMessage": "addComment did not store the comment" },
    { "name": "settle", "wait": 1200 },
    { "name": "shot-panel", "screenshot": "comments-panel" },
    { "name": "resolve", "eval": "(()=>{const s=window.__store.getState(); s.setCommentResolved(s.comments[0].id, true);})()" },
    { "name": "assert-resolved", "waitFor": { "store": "state.comments[0].resolved === true" }, "timeout": 5000, "failMessage": "setCommentResolved did not resolve the comment" },
    { "name": "mobile", "viewport": { "width": 390, "height": 844 } },
    { "name": "settle-mobile", "wait": 1000 },
    { "name": "shot-mobile", "screenshot": "comments-mobile" },
    { "name": "close-panel", "store": { "action": "setCommentsOpen", "args": [false] } },
    { "name": "panel-gone", "waitFor": { "css": "#commentsPanel", "visible": false }, "timeout": 5000 },
    { "name": "back-to-simple", "eval": "(()=>{const s=window.__store.getState(); s.setUiMode('simple'); s.reresolveFeatureFlags();})()" },
    { "name": "assert-simple-again", "waitFor": { "store": "state.featureFlags.comments === false" }, "timeout": 5000 }
  ]
}
